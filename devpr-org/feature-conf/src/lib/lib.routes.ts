import { Route } from '@angular/router';
import { FeatureConfComponent } from './feature-conf.component';

export const featureConfRoutes: Route[] = [
  { path: '', component: FeatureConfComponent },
];
